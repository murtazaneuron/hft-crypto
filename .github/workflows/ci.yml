name: CI

on:
    push:
        branches: [main, master]
    pull_request:
        branches: [main, master]

env:
    CARGO_TERM_COLOR: always
    CARGO_INCREMENTAL: 0 # disable incremental; full clean builds on CI
    RUST_BACKTRACE: short
    RUST_LOG: hft_crypto=debug

# Cancel in-flight runs for the same branch on new push
concurrency:
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

jobs:
    # ── 1. Format ───────────────────────────────────────────────────────────────
    fmt:
        name: Rustfmt
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Install Rust (nightly + rustfmt)
              # rustfmt.toml uses edition = "2024" which requires nightly rustfmt
              uses: dtolnay/rust-toolchain@nightly
              with:
                  components: rustfmt

            - name: cargo fmt --check
              run: cargo fmt --all -- --check

    # ── 2. Clippy ───────────────────────────────────────────────────────────────
    clippy:
        name: Clippy (${{ matrix.features }})
        runs-on: ubuntu-latest
        strategy:
            fail-fast: false
            matrix:
                include:
                    - features: default
                      cargo_args: --all-targets
                    - features: ai-agent
                      cargo_args: --all-targets --features ai-agent
        steps:
            - uses: actions/checkout@v4

            - name: Install Rust (MSRV)
              uses: dtolnay/rust-toolchain@1.93.1
              with:
                  components: clippy

            - uses: Swatinem/rust-cache@v2
              with:
                  key: clippy-${{ matrix.features }}

            - name: cargo clippy (strict, mirrors tasks.json)
              run: |
                  cargo clippy ${{ matrix.cargo_args }} -- \
                    -D warnings \
                    -A clippy::module_name_repetitions \
                    -A clippy::must_use_candidate \
                    -A clippy::missing_errors_doc

    # ── 3. Tests ───────────────────────────────────────────────────────────────
    test:
        name: Test (${{ matrix.os }} · ${{ matrix.toolchain }} · ${{ matrix.features }})
        runs-on: ${{ matrix.os }}
        strategy:
            fail-fast: false
            matrix:
                os: [ubuntu-latest, macos-latest]
                toolchain: [stable, "1.93.1"] # latest stable + MSRV
                features: [default, ai-agent]
                include:
                    # Only run the full matrix on Linux; macOS covers stable/default only
                    - os: macos-latest
                      toolchain: stable
                      features: default
                exclude:
                    - os: macos-latest
                      toolchain: "1.93.1"
                    - os: macos-latest
                      features: ai-agent
        steps:
            - uses: actions/checkout@v4

            - name: Install Rust (${{ matrix.toolchain }})
              uses: dtolnay/rust-toolchain@master
              with:
                  toolchain: ${{ matrix.toolchain }}

            - uses: Swatinem/rust-cache@v2
              with:
                  key: test-${{ matrix.os }}-${{ matrix.toolchain }}-${{ matrix.features }}

            - name: Build (default features)
              if: matrix.features == 'default'
              run: cargo build --all-targets

            - name: Build (ai-agent feature)
              if: matrix.features == 'ai-agent'
              run: cargo build --all-targets --features ai-agent

            # ── Core test suites ────────────────────────────────────────────────────
            - name: Tests — crypto primitives (ECDSA · Ed25519 · HMAC)
              run: cargo test --test crypto_tests -- --nocapture

            - name: Tests — 7-exchange auth adapters
              run: cargo test --test exchange_auth_tests -- --nocapture

            - name: Tests — doctests + unit tests (default features)
              if: matrix.features == 'default'
              run: cargo test --workspace -- --test-threads=8

            - name: Tests — doctests + unit tests (ai-agent feature)
              if: matrix.features == 'ai-agent'
              run: cargo test --workspace --features ai-agent -- --test-threads=8

    # ── 4. Release build ────────────────────────────────────────────────────────
    build-release:
        name: Release build (Linux · stable)
        runs-on: ubuntu-latest
        needs: [fmt, clippy, test]
        steps:
            - uses: actions/checkout@v4

            - name: Install Rust (stable)
              uses: dtolnay/rust-toolchain@stable

            - uses: Swatinem/rust-cache@v2
              with:
                  key: release

            - name: cargo build --release
              # Mirrors tasks.json: CARGO_INCREMENTAL=0, LTO already on in profile
              run: cargo build --release
              env:
                  CARGO_INCREMENTAL: 0

            - name: Upload release binary
              uses: actions/upload-artifact@v4
              with:
                  name: hft-crypto-linux-x86_64
                  path: target/release/hft-crypto
                  retention-days: 7

    # ── 5. Docs ─────────────────────────────────────────────────────────────────
    docs:
        name: Rustdoc (docs.rs parity)
        runs-on: ubuntu-latest
        needs: [fmt, clippy, test]
        steps:
            - uses: actions/checkout@v4

            - name: Install Rust
              uses: dtolnay/rust-toolchain@stable
              with:
                  key: docs

            - name: Cache cargo registry
              uses: actions/cache@v4
              with:
                  path: |
                      ~/.cargo/registry
                      ~/.cargo/git
                      target
                  key: ${{ runner.os }}-cargo-doc-${{ hashFiles('**/Cargo.lock') }}
                  restore-keys: ${{ runner.os }}-cargo-doc-

            - name: cargo doc --no-deps (warnings as errors)
              # Matches [package.metadata.docs.rs] rustdoc-args in Cargo.toml.
              run: cargo doc --no-deps
              env:
                  RUSTDOCFLAGS: "--cfg docsrs -D warnings"

    # ── 6. MSRV check ───────────────────────────────────────────────────────────
    msrv:
        name: MSRV (rust-version = 1.93.1)
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4

            - name: Install MSRV toolchain
              uses: dtolnay/rust-toolchain@1.93.1

            - uses: Swatinem/rust-cache@v2
              with:
                  key: msrv

            - name: cargo check (MSRV, all targets)
              run: cargo check --all-targets

            - name: cargo check (MSRV, ai-agent feature)
              run: cargo check --all-targets --features ai-agent

    # # ── 7. Live Anthropic provider (manual / scheduled only) ────────────────────
    # provider-live:
    #     name: Anthropic live provider test
    #     runs-on: ubuntu-latest
    #     # Only run when ANTHROPIC_API_KEY is available — skip on forks / external PRs
    #     if: |
    #         github.event_name == 'push' &&
    #         (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    #     needs: [test]
    #     steps:
    #         - uses: actions/checkout@v4

    #         - name: Install Rust (stable)
    #           uses: dtolnay/rust-toolchain@stable

    #         - uses: Swatinem/rust-cache@v2
    #           with:
    #               key: provider-live

    #         - name: Run ignored provider tests
    #           run: |
    #               cargo test \
    #                 --test providers \
    #                 --features ai-agent \
    #                 -- --ignored --test-threads=1 --nocapture
    #           env:
    #               ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
    #               RUST_LOG: hft_crypto=debug,rig_core=info
    #               RUST_BACKTRACE: 1
